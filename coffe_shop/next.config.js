/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['i.pinimg.com', 'skdw-my.sharepoint.com'], // เพิ่มทุกโดเมนรูปที่คุณใช้
    },
  };
  
  module.exports = nextConfig;
  